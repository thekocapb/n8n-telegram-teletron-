# 🚀 Telegram API для n8n (Telethon Worker)

Полнофункциональный Telegram API через **FastAPI** и **Telethon** для интеграции с **n8n workflows**.

## 📋 Описание проекта

Проект представляет собой микросервисную архитектуру, состоящую из двух основных компонентов:

1. **n8n** — платформа автоматизации рабочих процессов (workflows)
2. **telegram_worker** — FastAPI сервис, предоставляющий REST API для работы с Telegram через Telethon

### Как это работает

```
┌─────────────┐         HTTP Request          ┌──────────────────┐
│     n8n     │───────────────────────────────>│ telegram_worker  │
│  (workflow) │                                 │   (FastAPI)      │
└─────────────┘                                 └────────┬─────────┘
                                                          │
                                                          │ Telethon
                                                          ▼
                                                  ┌───────────────┐
                                                  │   Telegram    │
                                                  │      API      │
                                                  └───────────────┘
```

**Архитектура:**

1. **n8n** запускается на порту `5678` и позволяет создавать автоматизированные workflows
2. **telegram_worker** — это FastAPI приложение на порту `8099`, которое:
   - Использует библиотеку **Telethon** для подключения к Telegram API
   - Хранит сессию авторизации в файле `/app/session/bot.session`
   - Предоставляет REST API endpoints для работы с Telegram
   - Доступен из n8n через Docker network (`telegram_worker:8099`)

3. При запуске `telegram_worker` автоматически подключается к Telegram используя сохраненную сессию
4. n8n может отправлять HTTP-запросы к `telegram_worker` для выполнения операций с Telegram

## ✨ Функции

- ✅ **`/send_message`** — отправка сообщений в чаты, личные сообщения или каналы
- ✅ **`/chats`** — получение списка всех чатов с ID для использования в workflows
- ✅ **`/qr`** — генерация QR-кода в формате base64
- ✅ **`/debug_entity/{id}`** — отладка и проверка доступности чата/пользователя по ID
- ✅ **`/health`** — проверка состояния сервиса и авторизации
- ✅ Глобальный Telethon client — единое подключение для всех запросов
- ✅ Автоматическая инициализация при старте

## 🛠 Быстрый старт

### 1. Получите Telegram API ключи

1. Перейдите на https://my.telegram.org
2. Войдите в свой аккаунт
3. Перейдите в раздел **API development tools**
4. Создайте новое приложение (если еще не создавали)
5. Скопируйте `api_id` и `api_hash`

### 2. Настройка окружения

```bash
# Клонируйте репозиторий
git clone https://github.com/thekocapb/n8n-telegram-teletron-
cd n8n-telegram-teletron-

# Скопируйте файл с переменными окружения
cp .env.example .env

# Отредактируйте .env и укажите свои ключи
# TG_API_ID=12345678
# TG_API_HASH=abcdef1234567890abcdef1234567890
```

### 3. Создание Docker network (если еще не создан)

```bash
docker network create proxy-net
```

### 4. Авторизация в Telegram (ОДИН РАЗ)

Перед первым запуском необходимо создать сессию:

```bash
docker compose run --rm telegram_worker python
```

В Python консоли выполните:

```python
from telethon.sync import TelegramClient
import os

api_id = int(os.getenv("TG_API_ID", "0"))
api_hash = os.getenv("TG_API_HASH", "")

client = TelegramClient('/app/session/bot.session', api_id, api_hash)
client.start()  # Введите номер телефона, затем код из Telegram
exit()
```

**Важно:** Сессия сохраняется в `telegram_worker/session/`, не удаляйте эту папку!

### 5. Запуск проекта

```bash
docker compose up -d
```

Сервисы будут доступны:
- **n8n**: http://localhost:5678
- **telegram_worker API**: http://localhost:8099
- **API документация**: http://localhost:8099/docs

## 🧪 Тестирование API

### Health check

```bash
curl http://localhost:8099/health
```

Ответ:
```json
{
  "ok": true,
  "authorized": true
}
```

### Получить список чатов

```bash
curl http://localhost:8099/chats
```

Ответ:
```json
[
  {
    "id": "123456789",
    "title": "Мой чат",
    "type": "group"
  }
]
```

### Отправка сообщения

```bash
curl -X POST http://localhost:8099/send_message \
  -H "Content-Type: application/json" \
  -d '{
    "to": "123456789",
    "message": "🤖 Hello from n8n!"
  }'
```

**Поддерживаемые форматы `to`:**
- ID чата: `"123456789"`
- Username: `"@username"`
- Ключевое слово: `"me"` (себе)

### Генерация QR-кода

```bash
curl -X POST http://localhost:8099/qr \
  -H "Content-Type: application/json" \
  -d '{"text":"https://t.me/username"}'
```

Ответ:
```json
{
  "png_base64": "iVBORw0KGgoAAAANSUhEUgAA..."
}
```

### Отладка entity

```bash
curl http://localhost:8099/debug_entity/123456789
```

## 📊 Использование в n8n

### Пример workflow

1. **Webhook Trigger** — настройте webhook для приема запросов
2. **HTTP Request Node** — добавьте узел для вызова telegram_worker:
   - **Method**: POST
   - **URL**: `http://telegram_worker:8099/send_message`
   - **Body**:
     ```json
     {
       "to": "{{ $json.chat_id }}",
       "message": "{{ $json.message }}"
     }
     ```

### Получение ID чата для использования

1. Вызовите `/chats` endpoint из n8n
2. Найдите нужный чат в списке
3. Используйте его `id` в workflows

## 📁 Структура проекта

```
.
├── docker-compose.yml          # Оркестрация сервисов
├── telegram_worker/
│   ├── Dockerfile              # Образ для telegram_worker
│   ├── app.py                  # FastAPI приложение
│   └── session/                # Сессия Telethon (создается автоматически)
├── n8n_data/                   # Данные n8n (volumes)
├── .env.example                # Пример переменных окружения
└── README.md                   # Документация
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `TG_API_ID` | ID приложения из my.telegram.org | Да |
| `TG_API_HASH` | Hash приложения из my.telegram.org | Да |
| `WEBHOOK_URL` | URL для n8n webhooks | Нет |
| `N8N_SECURE_COOKIE` | Использовать secure cookies | Нет |

### Порты

- `5678` — n8n веб-интерфейс
- `8099` — telegram_worker API (доступен только через Docker network или localhost)

## 💡 Решение проблем

### ❌ "Не авторизован" при запуске

**Решение:** Выполните авторизацию вручную:
```bash
docker compose run --rm telegram_worker python
# Следуйте инструкциям выше в разделе "Авторизация в Telegram"
```

### ❌ 500 EntityNotFound

**Причина:** Неверный ID чата или чат недоступен

**Решение:** 
1. Проверьте список чатов: `curl http://localhost:8099/chats`
2. Используйте `/debug_entity/{id}` для проверки доступности

### ❌ "Port busy" при запуске

**Решение:**
```bash
docker compose down
# Проверьте, что порты 5678 и 8099 свободны
docker compose up -d
```

### ❌ "client not initialized"

**Причина:** Не указаны `TG_API_ID` или `TG_API_HASH`

**Решение:** Убедитесь, что в `.env` файле указаны корректные значения

### ❌ Сессия потеряна

**Причина:** Удалена папка `telegram_worker/session/`

**Решение:** Выполните авторизацию заново (см. раздел "Авторизация в Telegram")

## 🔐 Безопасность

- ⚠️ Не коммитьте файл `.env` в репозиторий
- ⚠️ Не публикуйте `TG_API_HASH` и сессию
- ⚠️ telegram_worker доступен только через Docker network или localhost
- ✅ Сессия хранится в локальном volume

## 📚 API Документация

Интерактивная документация доступна по адресу:
- **Swagger UI**: http://localhost:8099/docs
- **ReDoc**: http://localhost:8099/redoc

## 🚀 Примеры использования

### Автоматическая отправка уведомлений

Создайте workflow в n8n, который:
1. Отслеживает события (webhook, cron, и т.д.)
2. Вызывает `/send_message` для отправки уведомлений

### Мониторинг и алертинг

Настройте интеграцию с системами мониторинга для отправки алертов в Telegram

### Массовая рассылка

Используйте n8n для создания workflow массовой рассылки сообщений

## 📝 Лицензия

Проект предоставляется "как есть" для использования.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs telegram_worker`
2. Убедитесь, что сессия авторизована
3. Проверьте правильность API ключей в `.env`
